{
  "name": "@cataqui/protomap-worker",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@11.4.0",
  "scripts": {
    "start": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "build": "wrangler deploy --outdir dist --dry-run",
    "lint": "biome check src/",
    "format": "biome format --write src/",
    "biome": "biome check --write src/",
    "biome-check": "biome check src/",
    "prepare": "husky",
    "r2:bucket:create:dev": "wrangler r2 bucket create protomap-development",
    "r2:bucket:create:staging": "wrangler r2 bucket create protomap-staging",
    "r2:bucket:create:prod": "wrangler r2 bucket create protomap-production",
    "r2:bucket:create:all": "pnpm r2:bucket:create:dev && pnpm r2:bucket:create:staging && pnpm r2:bucket:create:prod"
  },
  "dependencies": {
    "pmtiles": "^4.3.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.0.0",
    "@cloudflare/vitest-pool-workers": "^0.7.0",
    "@cloudflare/workers-types": "^4.20250601.0",
    "@commitlint/cli": "^21.0.2",
    "@commitlint/config-conventional": "^21.0.2",
    "husky": "^9.1.7",
    "typescript": "^5.8.0",
    "vitest": "^3.0.8",
    "wrangler": "^4.59.1"
  }
}
